# Issue S0.4: RLS Readiness Assessment

**Status**: ✅ Resolved  
**Date Resolved**: 2026-06-19

## Description

Assess PostgreSQL Row-Level Security (RLS) readiness. For each table:
- Does it have `organization_id` column? → Can RLS be applied?
- Are there supporting indexes? → Will RLS queries be fast?
- What is the implementation path?

## Resolution

See `docs/security/rls_readiness_report.md` for complete assessment.

**RLS Readiness Matrix**:
- ✅ READY (9 tables): AccountingEntry, InvoiceSyncQueue, ExternalRequestLog, Order, OrderItem, OrderReturn, Payment, StockMovement, ExternalServiceConfig
- ⚠️ PARTIAL (14 tables): Missing recommended indexes; architecturally ready but needs optimization

**Indexing Gaps**:
- 11 tables missing `(organization_id)` index
- Recommended indexes provided for S1 optimization

**Implementation Roadmap**:
- S1: Add missing indexes (prep for RLS)
- S2: Implement RLS policies + GUC context management

**RLS vs TenantManager**:
- Current (S0-S1): TenantManager (application-enforced, bypassable via `.all_objects`)
- Future (S2): RLS (database-enforced, cryptographic guarantee)

**Evidence**: `docs/security/rls_readiness_report.md`

**Commit**: `docs: resolve #S0.4 rls readiness and indexing assessment`
